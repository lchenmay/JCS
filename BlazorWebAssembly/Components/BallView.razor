@using WebLogics;

<style>

</style>

<div>
    <input type="text" 
        @bind="this.ball.name" 
        style="background-color: @this.ball.color" />
    <button @onclick="Faster">Faster</button>
    <button @onclick="Slower">Slower</button>
    <button @onclick="Enlarge">Enlarge</button>
    <button @onclick="Shrink">Shrink</button>
</div>

@code {
    [Parameter]
    public double r { get; set; } = 0.0;

    [Parameter]
    public Game.Ball ball { get; init; } = new(
            "White",
            "",
            5.0,
            false,
            0.0,
            0.0,
            0.0,
            0.0);
    // public Game.Ball? Ball { get; init; }

    public void Faster()
    {
        this.ball.vx *= 2.0;
        this.ball.vy *= 2.0;
    }

    public void Slower()
    {
        this.ball.vx /= 2.0;
        this.ball.vy /= 2.0;
    }

    public void Enlarge()
    {
        this.ball.r *= 2.0;
    }

    public void Shrink()
    {
        this.ball.r /= 2.0;
    }

}
